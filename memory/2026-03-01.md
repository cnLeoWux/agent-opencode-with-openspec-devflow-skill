# 2026-03-01

## opencode-openspec-workflow Skill 开发经验总结

### 事件回顾
1. Leo 要求补充 OpenSpec workflow skill 文档，特别是 OPSX-CONTINUE 的循环机制
2. 更新了 SKILL.md，明确 OPSX-CONTINUE 是循环过程，每次生成/更新一个工件
3. 通用化文档，去除个人化称呼（图灵 → AI，Leo → 用户）
4. 创建独立 repo: https://github.com/cnLeoWux/agent-opencode-with-openspec-devflow-skill
5. **错误**：第一次 push 把整个 workspace（包括 .openclaw/, memory/, develop/, AGENTS.md 等）都上传了
6. **修复**：清除所有无关文件，只保留 SKILL.md，强制 push 修正

### 经验教训

#### 1. Skill-only Repo 规范
- **正确做法**：skill 专用 repo 只应该包含 `SKILL.md` 一个文件
- **错误做法**：把整个 workspace 上传到 skill repo
- **OpenSkill 规范**：每个技能是独立目录，`SKILL.md` 是唯一必需文件

#### 2. GitHub Repo 类型区分
| Repo 类型 | 用途 | 应该包含的内容 |
|----------|------|---------------|
| Workspace Repo | 个人工作区 | 完整的开发 workspace |
| Skill Repo | 分享技能 | 只有 `SKILL.md` |

#### 3. git clean 重要性
- 创建新 repo 前先确认 `git status` 和 `git ls-tree HEAD`
- Push 前用 `git ls-tree HEAD` 检查远程实际内容
- 使用 `.gitignore` 避免意外包含无关文件

#### 4. 检查清单
创建 skill repo 后：
- [ ] `ls -la` 确认只有必要文件（SKILL.md）
- [ ] `git ls-tree HEAD` 检查本地 commit 内容
- [ ] Push 前用 `git remote -v` 确认远程地址正确
- [ ] Push 后在 GitHub 检查网页显示

### 修正后的 repo 结构
```
agent-opencode-with-openspec-devflow-skill/
└── SKILL.md  ← OpenSkill 规范，唯一必需文件
```

### Link
- Skill 文档: `/Users/leowu-macmini/.openclaw/workspace-coding/skills/opencode-openspec-workflow/SKILL.md`
- GitHub Repo: https://github.com/cnLeoWux/agent-opencode-with-openspec-devflow-skill
